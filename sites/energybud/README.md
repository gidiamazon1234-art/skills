# EnergyBud — Brand Site

A multi-page marketing + storefront site for **EnergyBud**, the 1-gallon (128 oz) motivational
water bottle. Built with the design system generated by the
[`ui-ux-pro-max`](../../.claude/skills/ui-ux-pro-max) skill.

## Pages

| File | Purpose |
|------|---------|
| `index.html` | Home / landing — hero, features, hydration **Timeline**, color variants, reviews, CTA |
| `product.html` | Product detail — gallery, color picker, quantity, add-to-cart, spec accordion |
| `about.html` | Brand story, values, stats |
| `cart.html` | Cart + demo checkout (reads `localStorage`) |

## Shared assets

- `assets/styles.css` — the full design system (tokens, components, light/dark themes)
- `assets/fonts.css` — Barlow + Barlow Condensed inlined as base64 `@font-face` (no external requests)
- `assets/app.js` — theme toggle, scroll reveal, color switchers, and a `localStorage` cart shared across pages

## Design system (from the `ui-ux-pro-max` skill)

- **Style:** Vibrant & Block-based — bold, high-contrast, large sections
- **Palette:** water blue `#0284C7` + cyan `#06B6D4` + teal accent `#0891B2`, with the real brand charcoal `#333333` from the EnergyBud logo kit as the ink/inverted band
- **Type:** Barlow Condensed (display, athletic) + Barlow (body)
- **Motion:** staggered scroll reveals, animated hero water-fill, 150–300ms hover transitions
- **A11y:** WCAG-AA contrast, visible focus, `prefers-reduced-motion`, responsive at 375 / 768 / 1024 / 1440

## Run locally

It's static — open `index.html` directly, or serve the folder:

```bash
cd sites/energybud
python3 -m http.server 8000
# visit http://localhost:8000
```

## Notes

Real brand assets in use: the logo mark (`assets/logo-mark.png`, from the brand kit), the real
retail price ($39.95), the real Amazon rating (4.4 / 1,075 ratings), the real product title, the
real colourways, and the **actual motivational phrases printed on the bottle** (Rise & Grind →
Nearly There!) which drive the Timeline section.

## Product photography

Every product visual is a **photo slot**: it ships an inline-SVG illustration and automatically
upgrades to a real photo the moment the file exists. Drop your images into `assets/img/` using
the filenames listed in [`assets/img/README.md`](assets/img/README.md) — no code changes needed.

Mechanically, `app.js` HEAD-probes each `.shot[data-photo]` and only injects an `<img>` when the
request succeeds, so a missing photo never shows a broken-image icon. (The browser still records
one 404 per missing file in the Network tab; that disappears once the photos are uploaded.)

Still placeholder: **customer reviews are invented** and clearly marked as illustrative in the
footer — swap them for real reviews before production. Tritan™ is a trademark of Eastman Chemical Company.
